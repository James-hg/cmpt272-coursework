# A3

This TypeScript-based web application allows users to build recipes with real-time nutritional tracking and storage. It supports specialized recipe types like Vegetarian (blocking meat ingredients) and High Protein (warning if protein ratio is low). All data is persisted locally in the browser's `LocalStorage`.

---

I used ChatGPT to come up with pantry details in data/pantry.ts
